# Dotfiles

My `.config` dotfiles

## Setup

Clone repository using:

```
git clone https://github.com/EfJot314/dotfiles ~/.config
```

> [!NOTE]
> Repository is not well documented, so it is very likely that you will need to install/organize most dependencies yourself (will be fixed in the future)
